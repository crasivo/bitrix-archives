name: Scheduled publish all distros

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual maintenance'

jobs:
  publish_distro:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        distro: [
          start,
          standard,
          bitrix24,
          bitrix24_shop,
          bitrix24_enterprise,
          small_business,
          business,
          business_cluster,
          business_cluster_postgresql
        ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download distros & create manifest
        id: analysis
        run: |
          chmod +x ./bin/download-distro.sh
          ./bin/download-distro.sh ${{ matrix.distro }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: env.SKIP_PUBLISH != 'true' && env.BITRIX_SM_VERSION != '' && env.BITRIX_SM_VERSION != 'null'
        with:
          tag_name: ${{ env.BITRIX_RELEASE_TAG }}
          name: "Distro: ${{ matrix.distro }} v${{ env.BITRIX_SM_VERSION }}"
          body: |
            ## 📦 Информация о релизе

            Данный релиз содержит оригинальные архивы для редакции `${{ matrix.distro }}` 1C-Bitrix,
            которые были загружены с официального сайта 1С-Битрикс.

            **Kernel (main):**
            - Version: `${{ env.BITRIX_SM_VERSION }}`
            - Release date: `${{ env.BITRIX_SM_VERSION_DATE }}`

            Дополнительную информацию по текущему релизу можно посмотреть в файле `manifest.json`.
          files: |
            ${{ env.BITRIX_OUTPUT_DIR }}/*

      - name: Send Telegram Notification
        if: success() && env.SKIP_PUBLISH != 'true'
        uses: jiacai2050/telegram-action@v1
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat-id: ${{ secrets.TELEGRAM_CHAT_ID }}
          parse-mode: html
          message: |
            🚀 <b>Опубликован новый дистрибутив!</b>

            📦 Редакция: <code>${{ matrix.distro }}</code>
            🏷️ Версия ядра (main): <code>${{ env.BITRIX_SM_VERSION }}</code>
            📅 Дата выпуска ядра (main): <code>${{ env.BITRIX_SM_VERSION_DATE }}</code>

            🔗 <a href="https://github.com/${{ github.repository }}/releases/tag/${{ env.BITRIX_RELEASE_TAG }}">Посмотреть релиз</a>
            🔗 <a href="https://github.com/${{ github.repository }}/releases/download/${{ env.BITRIX_RELEASE_TAG }}/manifest.json">Посмотреть manifest</a>

            #github #release #bitrix #distro #${{ matrix.distro }}
        env:
          BITRIX_RELEASE_TAG: ${{ env.BITRIX_RELEASE_TAG }}
          BITRIX_SM_VERSION: ${{ env.BITRIX_SM_VERSION }}
          BITRIX_SM_VERSION_DATE: ${{ env.BITRIX_SM_VERSION_DATE }}
