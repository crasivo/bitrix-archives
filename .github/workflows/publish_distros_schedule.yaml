name: Scheduled publish all distros

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual maintenance'

jobs:
  publish_distro:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        distro: [
          start,
          standard,
          bitrix24,
          bitrix24_shop,
          bitrix24_enterprise,
          small_business,
          business,
          business_cluster,
          business_cluster_postgresql
        ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download distros & create manifest
        id: analysis
        run: |
          chmod +x ./bin/download-distro.sh
          ./bin/download-distro.sh ${{ matrix.distro }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        # Публикуем только если это новая версия и она успешно определена
        if: env.SKIP_PUBLISH != 'true' && env.BITRIX_MAIN_VERSION != '' && env.BITRIX_MAIN_VERSION != 'null'
        with:
          tag_name: ${{ matrix.distro }}-${{ env.BITRIX_MAIN_VERSION }}
          name: "Distro: ${{ matrix.distro }} v${{ env.BITRIX_MAIN_VERSION }}"
          body: |
            ## 📦 Информация о релизе

            Данный релиз содержит оригинальные архивы для редакции `${{ matrix.distro }}` 1C-Bitrix,
            которые были загружены с официального сайта 1С-Битрикс.

            **Kernel (main):**
            - Version: `${{ env.BITRIX_MAIN_VERSION }}`
            - Release date: `${{ env.BITRIX_MAIN_VERSION_DATE }}`

            **Tar meta:**
            - Filename: `${{ matrix.distro }}.tar.gz`
            - MD5: `${{ env.BITRIX_META_TAR_MD5 }}`
            - SHA1: `${{ env.BITRIX_META_TAR_SHA1 }}`
            - SHA256: `${{ env.BITRIX_META_TAR_SHA256 }}`
            - Size: ${{ env.BITRIX_META_TAR_SIZE_MB }} Mb

            **Zip meta:**
            - Filename: `${{ matrix.distro }}.zip`
            - MD5: `${{ env.BITRIX_META_ZIP_MD5 }}`
            - SHA1: `${{ env.BITRIX_META_ZIP_SHA1 }}`
            - SHA256: `${{ env.BITRIX_META_ZIP_SHA256 }}`
            - Size: ${{ env.BITRIX_META_ZIP_SIZE_MB }} Mb

            Релиз содержит файл <a href="https://github.com/${{ github.repository }}/releases/download/${{ env.RELEASE_TAG }}/manifest.json">manifest.json</a>, в котором содержиться вся необходимая информация, включая основные мета-данные для каждого архива.

            ---
            Сгенерировано автоматическии через GitHub Action.
          files: |
            ${{ env.BITRIX_MANIFEST_FILEPATH }}
            ${{ env.BITRIX_META_ZIP_FILEPATH }}
            ${{ env.BITRIX_META_ZIP_FILEPATH }}.*
            ${{ env.BITRIX_META_TAR_FILEPATH }}
            ${{ env.BITRIX_META_TAR_FILEPATH }}.*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
