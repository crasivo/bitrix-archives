name: Scheduled publish all scripts

on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual maintenance'
      notify_telegram:
        description: 'Telegram notifications'
        required: true
        default: true

jobs:
  publish_scripts:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        script_name: [
          bitrixsetup.php,
          bitrix_server_test.php,
          restore.php
        ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Define global variables
        run: |
          echo "RELEASE_DATE=$(date +'%d.%m.%Y')" >> $GITHUB_ENV;

      - name: Download script & create manifest
        id: analysis
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_on: error
          command: |
            chmod +x ./bin/download-script.sh
            ./bin/download-script.sh ${{ matrix.script_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: env.SKIP_PUBLISH != 'true'
        with:
          tag_name: ${{ env.BITRIX_RELEASE_TAG }}
          name: "Script: ${{ matrix.script_name }} (${{ env.RELEASE_DATE }})"
          body: |
            ## 📦 Информация о релизе

            Данный релиз содержит оригинальный скрипт `${{ matrix.script_name }}` 1C-Bitrix,
            который был загружен с официального сайта 1С-Битрикс.

            Дополнительную информацию по текущему релизу можно посмотреть в файле `manifest.json`.
          files: |
            ${{ env.BITRIX_OUTPUT_DIR }}/*

      - name: Send Telegram Notification
        if: success() && env.SKIP_PUBLISH != 'true' && inputs.notify_telegram != 'false'
        uses: jiacai2050/telegram-action@v1
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat-id: ${{ secrets.TELEGRAM_CHAT_ID }}
          parse-mode: html
          message-file: ${{ env.BITRIX_MANIFEST_PATH }}
          message: |
            🚀 <b>Опубликован новый скрипт!</b>

            📦 Файл: <code>${{ matrix.distro }}</code>

            🔗 <a href="https://github.com/${{ github.repository }}/releases/tag/${{ env.BITRIX_RELEASE_TAG }}">Посмотреть релиз</a>

            #github #release #bitrix #script
        env:
          BITRIX_MANIFEST_PATH: ${{ env.BITRIX_MANIFEST_PATH }}
          BITRIX_RELEASE_TAG: ${{ env.BITRIX_RELEASE_TAG }}

      - name: Send Telegram Alert
        if: failure() && inputs.notify_telegram != 'false'
        uses: jiacai2050/telegram-action@v1
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat-id: ${{ secrets.TELEGRAM_CHAT_ID }}
          parse-mode: html
          message: |
            🚨 <b>Ошибка публикации релиза</b>

            📦 Файл: <code>${{ matrix.script_name }}</code>

            🔗 <a href="https://github.com/${{ github.repository }}/${{ github.action_path }}">Посмотреть релиз</a>

            #github #alert #bitrix #script
